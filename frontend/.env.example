# Local default — Vite frontend talks to the backend API.
# Local:  http://localhost:5000/api
# Prod:   https://api.example.com/api  (or /api if same-origin via nginx)

VITE_API_URL=http://localhost:5000/api
